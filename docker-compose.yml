services:
  qdrant:
    image: qdrant/qdrant
    container_name: chatpdf-qdrant
    restart: unless-stopped
    volumes:
      - qdrant_storage:/qdrant/storage
    ports:
      - "127.0.0.1:6333:6333"

volumes:
  qdrant_storage:
